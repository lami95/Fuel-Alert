# Web / Auth
SECRET_KEY=changeme_secret_key
ADMIN_USER=admin
ADMIN_PASS=adminpassword
ADMIN_EMAIL=admin@example.com

# SMTP
SMTP_SERVER=smtp.example.com
SMTP_PORT=587
SMTP_USER=smtp_user
SMTP_PASS=smtp_pass
EMAIL_FROM=alerts@example.com

# Intervall (Minuten)
CHECK_INTERVAL=30

# LDAP (Synology)
LDAP_URI=ldap://192.168.1.50
LDAP_BASE_DN=dc=example,dc=local
LDAP_BIND_DN=
LDAP_BIND_PASSWORD=
LDAP_USER_DN_FORMAT=uid={username},ou=users,dc=example,dc=local

# API endpoint
PRICE_API=https://prix-carburants.services.public.lu/graphql
